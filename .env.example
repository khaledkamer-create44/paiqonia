# رابط قاعدة بيانات PostgreSQL
DATABASE_URL="postgresql://user:password@host:5432/baiquniyyah"

# سرّ التشفير الخاص بـ NextAuth — ولّده بالأمر: openssl rand -base64 32
NEXTAUTH_SECRET="ضع-هنا-قيمة-عشوائية-طويلة"

# رابط الموقع بعد النشر (مهم في الإنتاج)
NEXTAUTH_URL="https://your-domain.com"
